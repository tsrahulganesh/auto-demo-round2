pipeline:
  name: auto2pipee
  identifier: auto2pipee
  projectIdentifier: Demo_gitrepo
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: rrahul
        repoName: auto-demo-round2
        build: <+input>
  stages:
    - stage:
        name: auto2pipeebuild
        identifier: auto2pipeebuild
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          buildIntelligence:
            enabled: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    shell: Bash
                    command: mvn clean install
                  description: run
